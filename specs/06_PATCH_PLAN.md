# ScreenSearch V2 Patch Plan

Last updated: 2026-06-22

Items are ordered. An agent may continue independent work around a blocked item but must not silently reorder product dependencies.

## Active

1. [x] Extend capture/domain contracts with an explicit image media type and evidence metadata.
2. [x] Implement and test a real Windows image capture adapter with foreground process/title metadata.
3. [x] Compose the real adapter in production; keep fake capture test-only.
4. [x] Add automatic capture scheduling and continuous durable analysis with graceful shutdown.
5. [x] Implement real offline Windows OCR returning positioned text and a stable provider revision.
6. [x] Extend persistence and IPC with screenshot evidence, asset authorization, metadata, provenance, and OCR bounds.
7. [x] Add evidence-only search and render real screenshots/results in the diagnostics surface.
8. [x] Add quantized MiniLM ONNX embeddings and activate its 384-dimensional revision.
9. [x] Add queue high/low-water backpressure, conservative perceptual deduplication, single-flight capture, and pre-persistence pause/self-exclusion policy.
10. [x] Benchmark hybrid ranking, exact-match boost, model-revision isolation, latency, CPU, memory, database size, and ten-million-row metadata growth on the named P1 reference machine.
11. [x] Implement persisted user-configurable exclusions, age retention, captured-asset disk budgets, storage metrics, transactional capture deletion, and durable unreferenced-asset cleanup.
12. [x] Generate exactly three grounded visual directions and obtain product-owner selection of Memory Timeline.
13. [x] Record the selected visual target and binding decisions in `docs/design`.
14. [x] Add tray lifecycle, a system-wide hotkey, and complete keyboard navigation; the selected search/timeline/evidence/settings interface is implemented and has passed visual QA.
15. [ ] Select/install a GGUF model and validate the implemented llama.cpp provider, citations, cancellation, and memory lifecycle.
16. [x] Move OCR, embedding, and generation execution behind the supervised model-worker boundary.
17. [x] Implement typed Windows automation emission behind existing approval/focus/abort gates, or keep the feature disabled if safety requirements cannot be proven.
18. [ ] Complete locked-session privacy handling plus remaining recovery, worker-crash, Windows end-to-end, security, packaging, and release-hardware soak verification; saturation, 10-million-row metadata scale, live evidence latency, and settings visual QA are complete.
19. [x] Rewrite the remaining plan so it lists only genuine gaps after the useful-answer pass; release review remains gated by items 15 and 18.

## Remaining release gaps after useful-answer pass

1. **Item 15: release answer model.** Still requires qualitative groundedness/citation scoring, product/legal model approval, and final acquisition/redistribution policy. Memory-pressure-triggered unload is now implemented (OS low-memory resource notification; live pressure path pending manual Windows attestation). Useful-answer planning, filtered retrieval, prompt metadata, plan IPC, UI think-span stripping, guided model setup, and content-free archive smoke are implemented but do not close the release-model decision.
2. **Item 18: release hardening.** Still requires locked-session privacy handling, remaining security/privacy review, packaging/signing, release-hardware soak, recovery drills, and public-release ownership. Guarded automation remains default-off; release policy must decide whether it ships enabled only by explicit opt-in.

## Notes

- 2026-06-21 P0/P1 review: items 1–13 remain closed. A review pass added explicit analysis failure-path tests (retry backoff, dead-letter promotion at `MAX_JOB_ATTEMPTS`, embedding-dimension rejection at the persistence and `process_one` layers) so items 4 and 8 now meet the success-and-failure test bar below. No item was reopened; no production code changed.
- 2026-06-21 P2 shell pass (item 14, partial): implemented and composed in production the system tray (live capture-state tooltip + status line, Pause/Resume, Open, Quit), a configurable system-wide summon hotkey (default `Ctrl+Shift+Space`, registered from Rust via `tauri-plugin-global-shortcut`, persisted as a shell-local JSON setting), hide-to-tray on window close, and complete keyboard navigation (timeline arrow/Home/End with roving tab index, ARIA tablist arrow keys, modal focus trap, Escape-to-close with focus restore, Ctrl+K search focus). Verified: `cargo fmt`/`clippy -D warnings`/`test --workspace`, `npm run lint`/`build`, browser-driven keyboard-navigation QA against the dev server, and a user-attested manual Windows `npm run tauri dev` session against a running daemon on 2026-06-22. **Item 14 is closed.** The full shell reference and completed manual verification checklist are in `docs/design/p2-shell.md`.
- 2026-06-21 P3 worker/model branch (items 15/16, implementation in progress): added a generation-model catalog migration, local GGUF import, explicit Hugging Face download, active-model selection, inactive deletion, answer-status completion states, and a model-worker named-pipe endpoint for OCR, embeddings, and generation. The daemon now launches the sibling model-worker and routes production model ports through it. Items 15 and 16 remain open until formatting, Clippy, tests, frontend checks, local candidate benchmarks, and live Windows worker validation pass.
- 2026-06-22 P3 review-and-harden pass (items 15/16): made the worker boundary genuinely supervised and gave the generation model a real memory lifecycle. Added daemon worker supervision with sliding-window bounded restarts (`RestartPolicy`) and a loud daemon exit on budget exhaustion; a per-instance parent lifeline pipe that ties the worker's lifetime to the daemon (orphan prevention, safe Rust, no new `unsafe`); an idle-timeout unload that issues a raw worker unload while preserving the catalog selection for lazy reload; a generation wall-clock deadline; a lock-free `is_loaded()` so health never blocks behind a running generation; and a worker-reported model-revision drift check (ADR 0002). Tunables are code constants — no new environment variables. Added CI tests (catalog invariants, domain validation, every `answer_status` branch, restart policy, deadline predicate) and a gated `apps/daemon/tests/worker_supervision.rs` (opt-in `SCREENSEARCH_RUN_WORKER_IT=1`, GGUF cases on `SCREENSEARCH_TEST_GGUF`) covering readiness, lifeline-exit, kill→restart recovery, cancellation, and idle unload. Verified on Windows: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (52 passed, 7 ignored gated), `npm run lint`, `npm run build` — all passed. **Items 15 and 16 stay open**: closing them still requires a live-Windows GGUF benchmark of a selected candidate and the GAP-002/GAP-003 model decisions. Remaining item-15 sub-task: **memory-pressure-triggered unload** (idle-timeout unload is done; the pressure-signal path needs a Windows memory-pressure source — tracked as GAP-008).
- 2026-06-22 Local generation throughput + chat-template fix (item 15, stays open): diagnosed a live "generation does nothing" report — a 3B reasoning GGUF loaded fully, then ran CPU-only on a hardcoded two threads with no chat template, so the answer pane appeared frozen and a *Reasoning* model never surfaced an answer. Fixed: physical-core thread budget (was `n_threads(2)`); the 120 s generation deadline is now armed after the model is resident (a slow cold load no longer returns an empty answer); the model's own chat template is applied before tokenizing (instruct/reasoning models get role markers + the assistant tag, base models fall back to the raw prompt); the evaluation context is raised to 4096 and the token cap to 768 so a reasoning model can think and still answer, with the daemon stamping `context_tokens` from the shared constant; plus content-free generation timing diagnostics. Verified: `cargo fmt --check`, `clippy -D warnings`, `cargo test --workspace` (52 passed, 7 ignored). GPU acceleration was explored and **descoped for that pass**; a later 2026-06-22 validation branch re-scoped it as opt-in Vulkan worker support. **Follow-up for item 15:** optionally hide a reasoning model's `<think>` span from the answer surface in the UI.
- 2026-06-22 PR #6 review follow-ups (model-management polish, items 15/16 stay open): addressed the remaining low/medium review items. Fixed: `delete_generation_model` now removes the empty per-model directory after the file; Hugging Face downloads reject `text/*` error pages (gated-model login walls) before writing. Reviewed and left as-is: stored `context_tokens` reflects the fixed evaluation context `GENERATION_CONTEXT_TOKENS` = 4096 (raised earlier the same day; this note originally said 2048 in error — corrected 2026-06-23), not a fabricated capability. Deferred polish follow-ups (none blocking the evidence/generation path): (a) **import/download progress streaming** — needs an additive IPC progress response variant plus Tauri/UI; large GGUF imports currently appear frozen until they complete; (b) **read the GGUF-declared `context_length`** and raise the evaluation window beyond the current fixed 4096; (c) **Hugging Face `revision` pin and gated-model token auth** for reproducible provenance and gated models (ties to GAP-003; today only `resolve/main` is fetched and provenance is captured after the fact via the stored BLAKE3 hash). Verified: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (52 passed, 7 ignored), `npm run lint`, `npm run build` — all clean.
- 2026-06-22 P2/P3 validation pass (items 14 and 16 closed; item 15 narrowed): item 14 was closed from user-attested manual Windows tray/hotkey/hide-to-tray verification. Local text-only GGUF smoke benchmarks passed for `Ministral-3-3B-Reasoning-2512-Q4_K_M.gguf` (5641 ms, 49 token pieces) and `NVIDIA-Nemotron3-Nano-4B-Q4_K_M.gguf` (7338 ms, 38 token pieces). The gated live worker suite passed for both candidates: readiness/lifeline, kill-then-restart recovery, generation after restart, cancellation health responsiveness, and idle unload. After the user re-scoped GPU use, GGUF generation gained an opt-in Vulkan worker feature that requests full llama.cpp layer offload when `supports_gpu_offload()` is true and falls back to CPU otherwise; local Vulkan build verification is blocked until the Vulkan SDK provides `VULKAN_SDK`. **Item 16 is closed.** **Item 15 stays open** for release model approval/acquisition (GAP-002/GAP-003), qualitative groundedness/citation scoring, memory-pressure-triggered unload (GAP-008), and release GPU packaging/validation where applicable.
- 2026-06-22 P4 guarded automation (item 17 closed): implemented the default-off production path for typed Windows automation. Added the approved design/ADR documentation; domain policy and canonical plan digest validation; migration `0007_guarded_automation.sql`; content-free automation settings/run persistence; an `AutomationRepository` port; daemon-owned `AutomationService`; protobuf/Tauri operations for status, enable, target capture, approve, execute, abort, reset, and safety heartbeat; native Windows UIA/keyboard emission with HWND/PID/executable identity checks and WTS unlocked-session checks; and the manual approval workflow in the desktop. Added CI-run policy/persistence/service/IPC/adapter tests plus an opt-in synthetic Win32 fixture (`SCREENSEARCH_RUN_AUTOMATION_IT=1 cargo test -p screensearch-windows guarded_windows_automation_fixture_exercises_native_paths -- --ignored --nocapture`) that verifies UIA invoke/set-value, UTF-16 typing, key-chord fallback, and foreground rejection without controlling unrelated applications. GAP-006 is resolved as included behind explicit default-off opt-in. P2/P3 gaps and release-hardening item 18 stay open.
- 2026-06-22 useful local answers pass (item 19 closed; items 15/18 stay open): implemented deterministic local-time query planning for the supplied Telegram/GitHub/Codex/Amazon prompts; added `SearchFilters`, `SearchPlan`, and `SearchOptions`; applied source/time metadata filters inside persistence before rank fusion; changed FTS to phrase/exact boosting plus OR fallback; supported metadata-only retrieval; emitted an additive plan IPC/Tauri event; enriched answer prompts with capture id, local timestamp/timezone, application, title, and OCR excerpt; hid model `<think>` spans in the UI; redesigned Settings around readiness, timezone basis, active/installed answer models, blank guided local GGUF import, advanced HF download, explicit retention/budget state, and conservative storage reset; and added `local_archive_answer_smoke_is_content_free` for the four acceptance prompts. Item 15 remains open for qualitative scoring, model approval/acquisition, and memory-pressure unload. Item 18 remains open for locked-session privacy, security, packaging/signing, and soak work.

- 2026-06-23 P2 shell review pass (item 14 stays closed): a scrupulous re-review of the closed product shell found one escaped defect — OCR highlight overlays were positioned by percentage of the `.capture-image` container while the image uses `object-fit: contain` inside fixed-aspect boxes, so overlays drifted off the text on any non-16:9 capture (ultrawide, portrait/rotated, or once the `max-height` clamp engaged). Fixed by measuring the rendered image rect with a `ResizeObserver` (`useContainedRect`) and positioning overlays in pixels against it; reworked the desktop preview fixture into a committed, self-contained synthetic SVG so `npm run dev` / browser QA actually exercises alignment on any clone (the original QA drew synthetic bounds over blank space against a gitignored real capture, which is why it shipped). Also added the missing guarded-automation dialog `Tab` focus trap (now a shared hook), stopped `Home`/`End` from also moving the evidence timeline while a detail tab is focused, made `aria-current` explicit, and documented the Tauri command boundary. Two agent-flagged items were rejected after verification (the existing `aria-current` was valid; `missing_docs` does not fire on the binary crate). Verified: `npm run lint`/`build`; `cargo fmt --check`/`clippy --workspace --all-targets -- -D warnings`/`test --workspace` (52 passed, 7 ignored gated); plus live Playwright browser QA of overlay alignment (large + thumbnail, letterbox offset applied, resize tracked), the tab/timeline key separation, and the dialog focus trap. A non-16:9 / portrait overlay-alignment check was added to the `docs/design/p2-shell.md` manual Windows runbook for user attestation. **Item 14 stays closed; no production behavior was reopened.**

- 2026-06-23 P3 constrained-synthesis hardening pass (item 15 narrowed): a scrupulous P3 review confirmed the generation plumbing, worker supervision, idle unload, citations, and no-evidence path are genuinely implemented, and closed the two engineering gaps the specs flagged open. (1) **GAP-008 memory-pressure unload:** the resident generation model now unloads on a Windows low-memory resource notification in addition to the idle timeout, satisfying spec §11's "idle timeout or memory-pressure signal." A safe `MemoryPressureMonitor` wraps `CreateMemoryResourceNotification`/`QueryMemoryResourceNotification` (all `unsafe` confined to `crates/windows`); the existing daemon idle-unload loop queries it each tick and unloads via the same raw worker-unload path; the decision is the pure, unit-tested `should_unload_generation`; no new env vars. The live pressure path is gated and needs manual Windows attestation. (2) **GAP-011 prompt hardening:** untrusted application/window-title/OCR metadata is sanitized before the prompt (`sanitize_untrusted_field`: control characters collapse to spaces, `[`/`]` defanged) so an adversarial title cannot start its own prompt line or forge a `[capture-id]` citation; covered by `prompt_neutralizes_adversarial_metadata`. Also corrected a stale doc claim (the fixed context is 4096, not 2048). The model-revision-isolation invariant was confirmed already covered by `hybrid_ranking_boosts_exact_text_and_excludes_other_model_revisions`, so no duplicate test was added. GAP-002/GAP-003 (model approval/acquisition), GAP-005 (signing), qualitative scoring, and item 18 remain out of engineering scope. Verified: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`. **Item 15 stays open** for qualitative scoring and the GAP-002/GAP-003 model decisions; item 18 unchanged.

- 2026-06-23 P4 guarded-automation review pass (item 17 stays closed): a scrupulous re-review of the closed guarded-automation feature found one escaped, feature-breaking defect and three correctness/robustness gaps, all now fixed on a branch. (1) **Approval was unreachable through the UI:** the daemon's approval-time foreground-identity check is spec-correct, but the `approve_automation` Tauri command — unlike capture and execute — never hid the ScreenSearch window, so ScreenSearch itself was foreground at approve time and every approval returned `target_changed`. Fixed by routing capture/approve/execute through one shared `with_foreground_yielded` helper; added a daemon-layer regression test (`approval_requires_the_target_to_be_foreground_not_the_shell`). It escaped because service tests use a fake platform that always returns a matching target and the gated Win32 fixture calls `execute_action` directly, bypassing approval. (2) **Abort-shortcut fail-open:** the shell trusted a one-time startup registration snapshot; it now re-asserts the `Ctrl+Alt+Shift+Esc` registration each heartbeat and notifies once on loss, and the status carries `heartbeat_fresh`/`abort_registered` so the UI distinguishes "hotkey unavailable" from "reconnecting". (3) **Digest bound a volatile field:** the canonical plan digest hashed `display_title`, which `matches_identity` excludes; it now hashes identity (lowercased executable) + actions only, so a retitle can't force a `plan_mismatch`. (4) **Key-mapping drift:** UI↔wire↔domain key/modifier conversion is centralized in `screensearch-ipc::convert` with the token vocabulary single-sourced in the domain and exhaustive domain↔wire matches; a round-trip test covers every variant. Verified: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (all pass; gated native/scale tests remain `#[ignore]`), `npm run lint`, `npm run build`. **Item 17 stays closed; no production safety invariant was weakened.** A manual end-to-end approve→execute attestation on Windows remains the only check automated tests cannot cover and is recorded in `docs/design/p4-guarded-automation.md`.

## Definition for closing an item

An item is closed only when its production path is composed, tests cover success and failure behavior, user-visible claims are truthful, and the build review/changelog are updated. A placeholder, fake, unreachable adapter, or compile-only implementation does not close an item.
